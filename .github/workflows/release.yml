name: Build & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: win32
          - os: macos-latest
            platform: darwin

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - run: npm ci

      - name: Package application
        env:
          DEBUG: electron-packager*
        run: npm run package

      - name: Verify package output
        shell: bash
        run: |
          find out -maxdepth 2 -type d
          test -n "$(find out -mindepth 1 -maxdepth 1 -type d -print -quit)"
          wc -c .vite/build/main.js
          test "$(wc -c < .vite/build/main.js)" -lt 200000

      - name: Create installers
        run: npx electron-forge make --skip-package

      - name: List build output
        shell: bash
        run: find out -type f 2>/dev/null | head -80 || true

      - name: Verify Windows artifacts
        if: matrix.platform == 'win32'
        shell: bash
        run: |
          test -f out/make/squirrel.windows/x64/QA-Test-Scenarios-Setup.exe
          test -n "$(find out/make/zip/win32/x64 -name '*.zip' -print -quit)"

      - name: Verify macOS artifacts
        if: matrix.platform == 'darwin'
        shell: bash
        run: |
          test -n "$(find out/make -name '*.dmg' -print -quit)"
          test -n "$(find out/make/zip/darwin/arm64 -name '*.zip' -print -quit)"

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: build-${{ matrix.platform }}
          path: |
            out/make/squirrel.windows/**/*
            out/make/zip/**/*
            out/make/**/*.dmg
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: List artifacts
        run: find artifacts -type f | head -50

      - uses: softprops/action-gh-release@v3
        if: startsWith(github.ref, 'refs/tags/')
        with:
          draft: false
          generate_release_notes: true
          fail_on_unmatched_files: false
          files: |
            artifacts/**/*
